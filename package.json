{
  "name": "stretchtherapy",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:doppler": "doppler run -- pnpm build",
    "deploy": "pnpm build:doppler && pnpm verify:analytics && wrangler pages deploy dist --project-name stretchtherapy --branch main",
    "verify:analytics": "node scripts/verify-analytics.mjs",
    "verify:live": "node scripts/check-live-analytics.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
